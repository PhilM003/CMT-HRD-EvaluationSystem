/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Sarabun', 'sans-serif'],
      },
      // -----------------------------------------------------------------------
      // นี่คือชุดสี Navy-Gold ที่ต้องเพิ่มเข้าไปเพื่อให้โค้ด App.jsx ทำงานได้สวยงาม
      // -----------------------------------------------------------------------
      colors: {
        primary: {
          navy: "#1E293B",   // น้ำเงินเข้ม (Slate-900) - ใช้เป็นสีหลัก ดูเป็นทางการ
          gold: "#D97706",   // ทองเข้ม (Amber-600) - ใช้ตัดขอบ หรือเน้นจุดสำคัญ
        },
        secondary: {
          silver: "#94A3B8", // เงิน (Slate-400) - ใช้กับเส้นขอบ ข้อความรอง
          cream: "#F8FAFC",  // ครีม/ขาวอมเทา (Slate-50) - ใช้เป็นพื้นหลังสบายตา
          darkgold: "#B45309", // ทองไหม้ - ใช้เมื่อเอาเมาส์ชี้ปุ่มทอง
        },
        accent: {
          royalblue: "#3B82F6", // น้ำเงินสด (Blue-500) - ใช้กับปุ่มกด หรือจุดที่ต้องการความสดใส
          sand: "#FEF3C7",      // ทราย (Amber-100) - ใช้เป็นพื้นหลังอ่อนๆ ของส่วนเน้น
        },
        neutral: {
          dark: "#0F172A",   // ดำเกือบสนิท - ตัวหนังสือหลัก
          medium: "#64748B", // เทากลาง - คำอธิบาย
          light: "#E2E8F0",  // เทาอ่อน - พื้นหลังตาราง
          white: "#FFFFFF",
        },
      }
    },
  },
  plugins: [],
}